/**
 * ImageFitly — Counter config
 * Fill in your deployed Cloudflare Worker URL below, then it goes live.
 */
window.COUNTER_CONFIG = {
  // ← 在这里填入你的 Cloudflare Worker 地址，例如：
  //   "https://img-counter.YOUR-SUB.workers.dev"
  //   "https://counter.your-custom-domain.com"
  // 留空则计数器自动隐藏，不影响页面其他功能。
  workerUrl: '',

  // 站点标识，用于区分不同网站（保持默认即可）
  site: 'imagefitly',
};
